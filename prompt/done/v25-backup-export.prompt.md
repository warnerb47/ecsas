# Context
Update export zip `libs\ecsas\feature-setting\src\lib\components\export-zip-card`
backup logic is implement in rust: `src-tauri\src\db\backup.rs`
# Instruction
- let the user select the location and the name of the zip backup. You can keep the default name and folder if user do not set it.
