import { Route } from '@angular/router';
import { FeatureApplication } from './feature-application/feature-application';

export const featureApplicationRoutes: Route[] = [
  { path: '', component: FeatureApplication },
];
